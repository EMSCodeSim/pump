{
  "name": "pump",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && mkdir dist && cp index.html about.html app-only.html app-only-presets.html how-to.html privacy-policy.html contact.html dist/ && cp manifest.json site.webmanifest dist/ && cp engine181.png fireopscalc.png fireopscalc2.png dist/ && cp -R js dist/js && cp -R ccs dist/ccs && cp -R assets dist/assets"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
